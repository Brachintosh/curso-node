// Recuperar argumentos de entrada
// console.log(process.argv);

// Recuperar el proceso y su salida
// process.exit(1);

// Controlar eventos del proceso
process.on('exit', () => {
  // Limpiar los recursos, ej.
})

// Current working directory
console.log(process.cwd())
